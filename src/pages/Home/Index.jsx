import React from "react";
import CoverImage from "../../components/CoverImage/Index";

const Index = ({ mode }) => {
  return (
    <div id="homepage">
      <CoverImage mode={mode} />
    </div>
  );
};

export default Index;
